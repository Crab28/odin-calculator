let sum = 0;
let operator = null;

function add(...args) {
    return args[0] + args[1];
}

function subtract(...args) {
    return args[0] - args[1];
}

function multiply(...args) {
    return args[0] * args[1];
}

function divide(...args) {
    return args[0] / args[1];
}
